# Render API URL including /api prefix
# Local dev: leave empty (Vite proxy handles /api)
# Production: https://your-service.onrender.com/api
VITE_API_URL=

# Media CDN (same host as backend VIDEO_BASE_URL) — serves images like videos
# Production: https://media.crazylabel.us
VITE_MEDIA_URL=
